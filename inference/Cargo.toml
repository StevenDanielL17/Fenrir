[package]
name = "fenrir-inference"
version = "0.1.0"
edition = "2021"
description = "On-chain ML inference contract for Fenrir risk scoring"

[dependencies]
polkavm-derive = "0.14"

[profile.release]
opt-level = "z"
lto = true
